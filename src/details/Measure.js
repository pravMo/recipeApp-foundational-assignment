import React from 'react';
import './details.css'

const Measure = (props) => {
    return (
        <div className='det-flex-fix'>
            <li> {props.name} </li>
        </div>
    )
}

export default Measure;